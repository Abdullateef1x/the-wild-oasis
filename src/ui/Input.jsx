import styled from "styled-components";

const Input = styled.input`
border: 1px solid var(--color-grey-sm);
border-radius: var(--color-grey-sm);
padding: 0.8rem 1.2rem;
    box-shadow: var(--shadow-sm);
`;


export default Input